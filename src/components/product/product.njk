{% macro product(props) %}
		<div class="product" data-target="product">
				<button data-target="remove" class="product__remove">
						<span class="sr-only">Remove item from the basket</span>
				</button>
				<figure class="product__figure">
						<picture>
								<source media="(-webkit-min-device-pixel-ratio: 1.5)" srcset="{{props.image.2x}}"/>
								<img class="product__image" src="{{props.image.1x}}" alt="{{props.image.alt}}"/>
						</picture>
						<figcaption class="product__caption">
								{{props.title}}
						</figcaption>
				</figure>
				<div class="product__settings">
						<div class="product__controls">
								<button data-target="control-decrease" class="product__control">
										<span class="sr-only">Decrese</span></button>
								<span data-target="amount" class="product__amount">1</span>
								<button data-target="control-increase" class="product__control product__control--increase">
										<span class="sr-only">Increse</span></button>
						</div>
						<div class="product__price">
								<span>$</span>
								<span data-target="price" class="product__total">{{props.price}}</span>
						</div>
				</div>
		</div>
{% endmacro %}
